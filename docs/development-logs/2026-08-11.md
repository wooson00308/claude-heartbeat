# 2026-08-11 개발 기록

## 모델 카탈로그와 실행 전 검증

- 범위: provider 모델 카탈로그, 실행 계획의 모델 상태, DB schema v4 교정, 실패 사유 보존, macOS universal 런타임 패키지.
- 결정: Codex는 로그인 계정의 `codex debug models` 공개 목록을 정본으로 삼고, Claude 별칭은 확인 불가 상태로 제공해 고급 직접 지정을 막지 않는다.
- 변경: 정확한 `codex + gpt-5.6`만 `gpt-5.6-sol`로 멱등 교정하고, 확인된 미지원 모델은 lease·쿼터·CLI 시작 전에 거절한다. provider의 중첩 오류는 민감정보 제거 뒤 `role_session` 실패 사유로 보존한다.
- 검증: Python 전체 316 passed, 8 skipped; Ruff와 diff 검사 통과. v0.8.3 universal2 실행 파일·psutil 양쪽 arm64/x86_64 확인, manifest와 Agent API v1 계약 검증 통과. 실제 계정 계획에서 개발자 1자리와 `gpt-5.6-sol / available` 확인.
- 후속: Workflow Labs 앱의 모델 선택·실패 안내·최소 런타임 연동은 해당 QA 작업이 수정 요청으로 `todo`에 돌아온 뒤 적용한다.

## 종료된 실행의 경과 시간 고정

- 범위: 성공·실패·취소·복구 필요 실행의 종료 시각 계약과 과거 기록 호환.
- 결정: 새 실행은 `finishedAt`을 상태 전환 시 저장하고, 필드가 없던 과거 종료 기록은 DB의 마지막 영속 갱신 시각을 조회 응답에만 사용한다. 과거 행은 다시 쓰지 않는다.
- 검증: store·dispatch 집중 검사 34 passed 뒤 전체 316 passed, 8 skipped. Ruff와 diff 검사 통과.

## 완료 뒤 거짓 실패 방지

- 재현: 실제 Codex 실행이 `completed`를 기록한 뒤 비구조화 표준 오류 때문에 빈 `failed` 사건으로 뒤집혔다. 작업과 lease는 정상 마감되어 있었다.
- 결정: 구조화 표준 출력만 계약 일치 여부를 가르고, 표준 오류 경고는 성공을 뒤집지 않는다. 실제 비정상 종료에서는 민감정보를 제거한 표준 오류 또는 종료 코드를 실패 사유로 남긴다.
- 검증: provider·dispatch 집중 검사 50 passed 뒤 전체 316 passed, 8 skipped. Ruff와 diff 검사 통과.
